1. 在a0-a7中存放参数，a2存放13
2. 汇编代码中，`main`函数进行了内联优化，`main`函数直接计算出了结果并储存
3. `0x630`
4. `0x30`，加上`0x600`即为`printf`的地址
5. 57616转化为十六进制为0xe110，0x00646c72小端存储为72-6c-64-00，转换为ASCII为rld与00（字符串结尾）。因此输出为He110 World
   大端存储时，57616不需改变，i应改为0x726c6400
6. 取决于a2中已有的数据，因为调用时需要两个参数，却只传入了一个